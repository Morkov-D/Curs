[project]
name = "curs-marking"
version = "0.1.0"
description = "Локальная система учета и печати кодов маркировки"
requires-python = ">=3.11"
dependencies = [
  "flet>=0.28.0",
  "sqlalchemy>=2.0.0",
]

[tool.black]
line-length = 100

